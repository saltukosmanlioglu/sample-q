import React from "react";

import * as Styled from "./Footer.styled";

const Footer: React.FunctionComponent = () => {
  return <Styled.Footer>Welcome to TV Shows Library</Styled.Footer>;
};

export default Footer;
